name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff linter
        run: ruff check backend/src/ --config pyproject.toml

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install pytest pytest-cov

      - name: Run tests with coverage
        working-directory: backend
        run: |
          pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing

      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        with:
          file: backend/coverage.xml
          fail_ci_if_error: false
        continue-on-error: true

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          extra_args: --only-verified

  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Run pip-audit on backend
        run: |
          pip install -r backend/requirements.txt
          pip-audit
        continue-on-error: true

  frontend-lint:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    # Only run if frontend directory exists with a build-based setup
    steps:
      - uses: actions/checkout@v4

      - name: Check if frontend needs build
        id: check_frontend
        run: |
          # Check if this is a build-based frontend (has package.json with build script)
          if [ -d "frontend" ] && [ -f "frontend/package.json" ] && grep -q '"build"' frontend/package.json; then
            echo "needs_build=true" >> $GITHUB_OUTPUT
          else
            echo "needs_build=false" >> $GITHUB_OUTPUT
            echo "Static frontend or no build script - skipping build step"
          fi

      - name: Setup Node.js
        if: steps.check_frontend.outputs.needs_build == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        if: steps.check_frontend.outputs.needs_build == 'true'
        working-directory: frontend
        run: npm ci

      - name: Run TypeScript check
        if: steps.check_frontend.outputs.needs_build == 'true'
        working-directory: frontend
        run: npm run typecheck || true

      - name: Build frontend
        if: steps.check_frontend.outputs.needs_build == 'true'
        working-directory: frontend
        run: npm run build

  placeholder-check:
    name: Check for Unreplaced Placeholders
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for template placeholders
        run: |
          # Find files with unreplaced placeholders (exclude specs/ templates and this check)
          FOUND=$(grep -r "{{[A-Z_]*}}" --include="*.py" --include="*.yaml" --include="*.yml" --include="*.json" --include="*.ts" --include="*.tsx" --include="*.md" . \
            --exclude-dir=specs \
            --exclude-dir=node_modules \
            --exclude-dir=.git \
            || true)

          if [ -n "$FOUND" ]; then
            echo "WARNING: Found unreplaced placeholders:"
            echo "$FOUND"
            # Don't fail - this is expected in the template repo
            # Uncomment the next line to fail in real projects:
            # exit 1
          else
            echo "No unreplaced placeholders found (or this is expected in template repo)"
          fi
